export const matematika = {
    add: '+',
    diff: '-',
    multi: '*',
    div: '/',
    mod: '%',    
};

/* function add(a, b) {
    return a + b;
}

function diff(a, b) {
    return a - b;
}

function multi(a, b) {
    return a * b;
}
*/
//arba uzrasome kompaktiskesniu budu:

const add = (a, b) => a + b;
const diff = (a, b) => a - b;
const multi = (a, b) => a * b;
const div = (a, b) => a / b;
const mod = (a, b) => a % b;